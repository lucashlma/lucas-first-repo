# 深圳前海通勤圈找房跟踪

本目录用于按既定购房策略持续跟踪房源，不是投资建议。

## 文件

- `STRATEGY.md`：预算、杠杆、板块与否决条件
- `WATCHLIST.md`：主盯小区、本周候选、行动清单
- `listings.csv`：可排序的房源状态表

## 使用方式

1. 每周刷新挂牌与成交，更新 `listings.csv` 的 `status` / `last_checked`
2. 看房后把现场评分和底价写回
3. 进入出价池的房源，`decision` 改为 `出价中` / `放弃` / `成交`
